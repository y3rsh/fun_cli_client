name: CI

on: 
  push:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: what to point at?
      run: alias python374='/opt/hostedtoolcache/Python/3.7.4'
    - name: alias?
      run: python374 --version
